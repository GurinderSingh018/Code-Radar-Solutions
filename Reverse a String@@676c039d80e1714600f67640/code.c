#include <stdio.h>
#include<string.h>

int main() {
    char a[50];
    scanf("%s",&a);
    char b[50];
    int c=strlen(a);
    for(int i=0;i<c;i++){
        b[n-i-1]=a[i];
        scanf("%d",&b[n=i-1]);
    }
    printf("%s",b);
    return 0;
}