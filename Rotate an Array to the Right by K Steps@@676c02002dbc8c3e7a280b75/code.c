#include <stdio.h>

int main() {
    int n;
    int arr[i];
    scanf("%d",&n);
    for(int i=0;i<n;i++){
        scanf("%d",&arr[i]);
    }

    int k,temp;
    scanf("%d",&k);
    for(int i=0;i<=k;i++){
        temp=arr[n-1];
        for(int j=)
    } 

    return 0;
}